// Expo resolves `react-native-continued-task` as a config plugin through this
// file. It points at the compiled plugin so consumers never need TypeScript.
module.exports = require('./plugin/build/index').default;
