<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application>
        <!--
          WorkManager declares SystemForegroundService but not a
          foregroundServiceType, and Android refuses to start a typed foreground
          service without one. Merging layers the attribute onto WorkManager's
          own declaration. Declared here so bare React Native apps work without
          the Expo config plugin; the plugin overrides the type when an app
          needs mediaProcessing or specialUse instead.
        -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>
</manifest>
