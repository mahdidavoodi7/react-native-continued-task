///
/// HybridContinuedTaskSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `ContinuedTaskState` to properly resolve imports.
namespace margelo::nitro::continuedtask { enum class ContinuedTaskState; }
// Forward declaration of `ListenerSubscription` to properly resolve imports.
namespace margelo::nitro::continuedtask { struct ListenerSubscription; }
// Forward declaration of `TaskStopEvent` to properly resolve imports.
namespace margelo::nitro::continuedtask { struct TaskStopEvent; }

#include <string>
#include "ContinuedTaskState.hpp"
#include "ListenerSubscription.hpp"
#include <functional>
#include "TaskStopEvent.hpp"

namespace margelo::nitro::continuedtask {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `ContinuedTask`
   * Inherit this class to create instances of `HybridContinuedTaskSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridContinuedTask: public HybridContinuedTaskSpec {
   * public:
   *   HybridContinuedTask(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridContinuedTaskSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridContinuedTaskSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridContinuedTaskSpec() override = default;

    public:
      // Properties
      virtual std::string getId() = 0;
      virtual std::string getTitle() = 0;
      virtual std::string getSubtitle() = 0;
      virtual ContinuedTaskState getState() = 0;
      virtual double getCompletedUnitCount() = 0;
      virtual double getTotalUnitCount() = 0;

    public:
      // Methods
      virtual void updateTitle(const std::string& title, const std::string& subtitle) = 0;
      virtual void setProgress(double completedUnitCount, double totalUnitCount) = 0;
      virtual void complete(bool success) = 0;
      virtual void cancel() = 0;
      virtual ListenerSubscription addOnStartListener(const std::function<void()>& listener) = 0;
      virtual ListenerSubscription addOnStopListener(const std::function<void(const TaskStopEvent& /* event */)>& listener) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "ContinuedTask";
  };

} // namespace margelo::nitro::continuedtask
