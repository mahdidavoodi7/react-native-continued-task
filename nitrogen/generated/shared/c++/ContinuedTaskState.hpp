///
/// ContinuedTaskState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::continuedtask {

  /**
   * An enum which can be represented as a JavaScript union (ContinuedTaskState).
   */
  enum class ContinuedTaskState {
    PENDING      SWIFT_NAME(pending) = 0,
    RUNNING      SWIFT_NAME(running) = 1,
    FINISHED      SWIFT_NAME(finished) = 2,
    STOPPED      SWIFT_NAME(stopped) = 3,
  } CLOSED_ENUM;

} // namespace margelo::nitro::continuedtask

namespace margelo::nitro {

  // C++ ContinuedTaskState <> JS ContinuedTaskState (union)
  template <>
  struct JSIConverter<margelo::nitro::continuedtask::ContinuedTaskState> final {
    static inline margelo::nitro::continuedtask::ContinuedTaskState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("pending"): return margelo::nitro::continuedtask::ContinuedTaskState::PENDING;
        case hashString("running"): return margelo::nitro::continuedtask::ContinuedTaskState::RUNNING;
        case hashString("finished"): return margelo::nitro::continuedtask::ContinuedTaskState::FINISHED;
        case hashString("stopped"): return margelo::nitro::continuedtask::ContinuedTaskState::STOPPED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum ContinuedTaskState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::continuedtask::ContinuedTaskState arg) {
      switch (arg) {
        case margelo::nitro::continuedtask::ContinuedTaskState::PENDING: return JSIConverter<std::string>::toJSI(runtime, "pending");
        case margelo::nitro::continuedtask::ContinuedTaskState::RUNNING: return JSIConverter<std::string>::toJSI(runtime, "running");
        case margelo::nitro::continuedtask::ContinuedTaskState::FINISHED: return JSIConverter<std::string>::toJSI(runtime, "finished");
        case margelo::nitro::continuedtask::ContinuedTaskState::STOPPED: return JSIConverter<std::string>::toJSI(runtime, "stopped");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert ContinuedTaskState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("pending"):
        case hashString("running"):
        case hashString("finished"):
        case hashString("stopped"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
